# Visualization code

Place visualization, replay, plotting, and rendering code under `src/viz/`.

Keep data loading and transformations in their domain modules; visualization
code should consume those public interfaces rather than duplicate them.
